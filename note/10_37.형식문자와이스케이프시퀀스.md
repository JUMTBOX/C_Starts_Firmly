### 10_37.형식 문자와 이스케이프 시퀀스

```C
int x = 10;
// 문자 상수를 화면에 출력
putchar("B");

// "\n"은 개행문자이므로 알파벳 문자가 화면에 보이는 것이 아님
putchar("\n");

// printf는 매개변수가 가변적이다..
printf("%c\n","A");

// %d라는 형식 문자에 맞추어 변수 x에 담긴 값(=정보)을 출력 .. printf 함수명 중 f가 format을 의미함
printf("x는 %d 입니다.", x);

```

<table>
    <thead> 
        <td>형식문자</td>
        <td>자료형</td>
        <td>형식문자</td>
        <td>자료형</td>
    </thead>
    <tbody>
        <tr>
            <td>%c</td>
            <td>int, char</td>
            <td>%d</td>
            <td>int</td>
        </tr>
        <tr>
            <td>%o</td>
            <td>int</td>
            <td>%u</td>
            <td>unsigned integer</td>
        </tr>
        <tr>
            <td>%x, %X</td>
            <td> </td>
            <td>%e, %E</td>
            <td> </td>
        </tr>
        <tr>
            <td>%f</td>
            <td>double</td>
            <td>%g</td>
            <td>double</td>
        </tr>
        <tr>
            <td>%s</td>
            <td>String</td>
            <td>%zd</td>
            <td>size_t</td>
        </tr>
        <tr>
            <td>%lld</td>
            <td> </td>
            <td>%l64u</td>
            <td></td>
        </tr>
    </tbody>
</table>

```C
// 실수 자료 형식의 메모리 크기(바이트 단위)를 출력
printf("%d\n", sizeof(123.456F)); // -> 4
printf("%d\n", sizeof(123.456));  // -> 8

// float 형식을 실수 형식으로 출력
printf("%f\n", 123.456F); // -> 123.456001

//double 형식을 실수 형식으로 출력
printf("%f\n",123.456); //double -> 123.456000
printf("%lf\n",123.456); // long double -> 123.456000
printf("%g\n",123.456);  // long double -> 123.456
```

```C
double dData = 123.456;

printf("%f, %f", dData,-123.456); // -> 4

// 소수점 2번째 자리에서 반올림하고 1자리까지 출력 -> 123.5
printf("%.1f \n", dData);
// 소수점 3번째 자리에서 반올림하고 2자리까지 출력 -> 123.46
printf("%.2f \n", dData);
// 소수점 4번째 자리에서 반올림하고 3자리까지 출력 -> 123.456
printf("%.3f \n", dData);

printf("%8d\n", 123);  // -> ____123

//소수점을 포함해 12자리로 출력. 단, 소수점 이하 4번째 자리에서 반올림하고 3자리까지 출력한다.
printf("%12.3f\n", dData); // -> _____123.456 ("."=소수점까지 포함하여 12자리이니 앞의 공백은 5자리)
printf("%012.3f\n", dData); // -> 00000123.456 (그래서 여기서는 0이 5개)
```
